#include <stdio.h>

int main()
{
    char c1 = 97; // a의 ASCII 코드값 97 저장
    char c2 = 98; // b의 ASCII 코드값 98 저장

    printf("%c, %d\n", c1, c1); // 출력 결과 : a, 97
    printf("%c, %d\n", c2, c2); // 출력 결과 : b, 98

    return 0;
}